# M1 验收记录（docs/acceptance.md §5 格式）

## OA1 — OpenAI 非流式端到端（真实 agy 二进制跑通一次并记录）

- **日期**：2026-08-31
- **Commit sha**：`a7f7f0e`（M1 收尾提交序列 d3bf836 → 06342e9 → a7f7f0e 之上，构建产物来自该工作树）
- **执行人**：sakiko（Claude Code 会话代执行，用户事先批准）
- **环境**：Windows 10 Pro 主机（非 Docker），Node 24，真实 agy 二进制 `C:\Users\a\AppData\Local\agy\bin\agy.exe` 版本 **1.1.22**（≥ MIN_AGY_VERSION 1.1.8，启动探针通过）
- **证据类型**：请求/响应转录 + 服务端 pino NDJSON 日志行（原文转录如下；日志中无 key/token 明文 — G4 同步验证）

### 服务启动

```
AGY_PROXY_API_KEY=local-test-key AGY_PROXY_DATA_DIR=%TEMP%\agy-oa1-run \
AGY_PROXY_PORT=18099 AGY_PROXY_HOST=127.0.0.1 node dist/index.js
```

```json
{"level":30,"agyBin":"C:\\Users\\a\\AppData\\Local\\agy\\bin\\agy.exe","agyVersion":"1.1.22","msg":"agy probe ok"}
{"level":30,"msg":"Server listening at http://127.0.0.1:18099"}
{"level":30,"port":18099,"host":"127.0.0.1","msg":"agy-proxy listening"}
```

### 请求 1 — 缺少鉴权头（负例，鉴权中间件验证）

```
POST /v1/chat/completions  (无 Authorization 头)
```

```json
{"error":{"message":"Missing API key. Pass it as `Authorization: Bearer <key>`.","type":"authentication_error","code":"invalid_api_key"}}
```
→ HTTP **401** ✓

### 请求 2 — OA1 正例（消耗一次真实 Antigravity 配额）

```
POST /v1/chat/completions
Authorization: Bearer local-test-key
Content-Type: application/json

{"model":"gemini-3.7-flash","messages":[{"role":"user","content":"Reply with exactly one word: PONG"}]}
```

HTTP **200**：

```json
{
  "id": "chatcmpl-StJnuPrcn2jT-788JfyNw5Ap",
  "object": "chat.completion",
  "created": 1788133903,
  "model": "gemini-3.7-flash",
  "choices": [{
    "index": 0,
    "message": { "role": "assistant", "content": "PONG\n" },
    "finish_reason": "stop",
    "logprobs": null
  }],
  "usage": {
    "prompt_tokens": 16168,
    "completion_tokens": 280,
    "total_tokens": 16448,
    "prompt_tokens_details": { "cached_tokens": 0 },
    "completion_tokens_details": { "reasoning_tokens": 278 }
  }
}
```

### 服务端日志（请求 2）

```json
{"level":30,"reqId":"5567cf15-…","req":{"method":"POST","url":"/v1/chat/completions"},"msg":"incoming request"}
{"level":30,"reqId":"5567cf15-…","model":"gemini-3.7-flash","promptTokens":16168,"completionTokens":280,"finishKind":"stop","msg":"request complete"}
{"level":30,"reqId":"5567cf15-…","res":{"statusCode":200},"responseTime":11525.04,"msg":"request completed"}
{"level":30,"ok":true,"code":"OK","durationMs":11530,"model":"gemini-3.7-flash","msg":"agy run finished"}
```

### OA1 验收点逐项核对（acceptance.md §2.1）

| 验收点 | 结果 |
|---|---|
| `id` 格式 | ✓ `chatcmpl-` + 24 base64url 字符 |
| `object:'chat.completion'` | ✓ |
| `created` | ✓ epoch 秒 |
| `model` 回显 | ✓ `gemini-3.7-flash` |
| `choices[0].message.role/content` | ✓ `assistant` / `"PONG\n"`（真实模型输出带尾换行，正常） |
| `finish_reason:'stop'` | ✓ |
| usage 三元组 | ✓ prompt 16168 / completion 280 / total 16448 = 和；details 对象齐全 |

### 备注

- 首次请求（PowerShell `-d $body` 内联 JSON）返回 400 "Body is not valid JSON" —
  PowerShell 5.1 对 curl.exe 的引号转义问题，非网关缺陷；改用 `--data-binary @file` 后通过。
  该 400 体本身即验证了错误处理路径（`invalid_request_error`）。
- 运行结束无残留 agy 进程（进程列表核查）。
- 日志全文无 `local-test-key`、无 Bearer/token 形态字符串（G4 验证点，fake key 流程完成）。
- M1 DoD（acceptance.md §3）四项至此全部满足；前三项（移植清单+sha 头、fake-agy 全模式、
  stdin EOF/看门狗/进程组 kill）在 M1 引擎移植阶段完成（716fc3f 及此前提交）。
