# M4 验收记录（docs/acceptance.md §5 格式）

## M4 管理 WebUI —— 演练记录（acceptance.md §3 M4 DoD）

- **日期**：2026-09-01
- **Commit sha**：`621fd94`（M4 全部代码 + 文档提交后的工作树）
- **执行人**：sakiko（可自动化部分由 Claude Code 会话代执行；全程 fake-agy 上游，未消耗真实 Antigravity 配额。浏览器人工项明确标注「待用户」）
- **环境**：Windows 10 Pro 主机（非 Docker），Node 24.13，`dist/index.js` + `web/dist`（`npm run build` 链式产物，Vite 生产构建，assets 指纹哈希）。上游为 `test/fake-agy.mjs` 经 `agy.cmd` shim 注入（清洗 PATH/LOCALAPPDATA，同 m2/m3 坑）；`AGY_PROXY_WEB_DIST` 显式指向 `web/dist`。
- **服务端启动**：

```
AGY_PROXY_DATA_DIR=%TEMP%\agy-m4-drill\data AGY_PROXY_PORT=18120 AGY_PROXY_HOST=127.0.0.1
AGY_PROXY_BIN=%TEMP%\agy-m4-drill\bin\agy.cmd AGY_PROXY_WEB_DIST=<repo>/web/dist
AGY_PROXY_ADMIN_PASSWORD=drill-admin node dist/index.js
```

### 演练记录（按 DoD 条目）

### ① WebUI 托管（服务端腿，可自动化部分）

- `GET /` → **200** `text/html; charset=utf-8`，`cache-control: no-cache`，主体即 Vite SPA 壳（`<html lang="zh-CN">`，标题 agy-proxy）；`GET /login` → **200** SPA index（dot-free fallback）。
- 哈希资产 `GET /assets/index-BRHJw4eV.js` → **200** + `cache-control: public, max-age=31536000, immutable` ✓。
- **API 404 回归栅栏**：`GET /v1/nope` → 404 application/json（OpenAI 体）；`GET /admin/nope` → 404 application/json —— 有 WebUI 根与无 WebUI 根（`AGY_PROXY_WEB_DIST=none`）逐字一致（自动化：test/static-web.test.ts 双姿态对照）。
- `AGY_PROXY_WEB_DIST=none` opt-out 开关：注册与 SPA fallback 双关闭，`/` 恢复协议 404。

### ② 未认证访问 → 401（自动核对；浏览器走查待用户）

- `GET /admin/events` 无 cookie → **401**（守卫链复用，文案逐字）；全部 `/admin/*` JSON 路由同姿态（自动化 admin 三件套 pin）。
- `POST /admin/login` 错误密码 → 401（300ms damping）；登录后 cookie 可用。

### ③ SSE 实时推送（仪表盘/日志页免刷新）

- `GET /admin/events`（带 cookie）→ `content-type: text/event-stream`；首帧：
  `event: snapshot id: 1 data: {"type":"snapshot",…,"seq":1}`（空池快照）。
- 快照期间发起 `POST /v1/chat/completions`（`x-request-id: drill-m4-1`）→ 200；SSE 流随后：
  `event: run id: 2 data: {"type":"run","at":…,"run":{"ok":true,"status":"OK","durationMs":60,"model":"gemini-3-6-flash","family":"google","reqId":"drill-m4-1","usage":{"promptTokens":10,"completionTokens":5,"reasoningTokens":2,"cacheReadTokens":3}},"seq":2}`
  —— run 事件与账本行同源同字段（reqId 对齐）✓。
- **Last-Event-ID 续传**：重连带 `last-event-id: 1` → 恰好重放 seq 2 之后的 `run` 帧（无 snapshot）；带 `last-event-id: 999`（越界）→ 仅 `event: snapshot id: 3` 回退 —— **snapshot XOR replay** 活体验证 ✓。

### ④ 设置写路径 + 重启 roundtrip

- `PUT /admin/settings {defaultModel, timeoutMs}` → 200，响应 `requested` 与 `effective` 一致；`%TEMP%\agy-m4-drill\data\gateway\runtime-overrides.json` 落盘：
  `{"defaultModel": "gemini-3.7-pro", "timeoutMs": 300000}`（原子写）。
- **boot #2**（同一数据目录）→ `/admin/settings`：`requested` 原样保留、`effective.defaultModel=gemini-3.7-pro`、`effective.timeoutMs=300000` ✓（resolveConfig 分层生效）。

### ⑤ 415（m3 整改项关闭）

- `Content-Type: application/x-www-form-urlencoded` POST `/admin/login` → **415**；POST `/v1/chat/completions` → **415**（m3 演练时同为 500）。响应消息含 `(content-type: application/x-www-form-urlencoded)`。

### ⑥ i18n / 主题 / 移动端 / Lighthouse / skip 二次确认 —— **待用户**

- i18n 完整性与硬编码 CJK 纪律已机械化：`web/src/i18n/completeness.test.ts`（zh/en 键集一致且非空）✓ 自动化；`grep -r "[一-龥]" web/src --include="*.tsx"` = 0 命中 ✓ 自动化。
- **待用户执行**：⑥a 六页面人工走查（登录→仪表盘→账号→Keys→用量→设置，zh/EN 切换、深浅主题、375px 截图）；⑥b Lighthouse a11y ≥90（生产构建 `npm run web:build` + 网关托管运行态，Chrome）；⑥c 未登录访问任意 admin API 的 401 可视化核对；⑥d skip 开关二次确认对话框人工走查（先在设置页选择 skip，确认对话框 + 输入 SKIP 才生效）。

### G2/G3/G4 复核

- G2 `grep -rn "\.skip\|\.todo" test/`：仅 `res.skipped` 字段断言（media 测试的字段名），0 个用例 skip ✓。
- G3 无 `@ts-ignore`/`@ts-expect-error`（src/ + web/src）✓。
- G4 `console.log`：src/ 0、web/src 0（CI 亦加栅栏）；演练日志双转录 grep `drill-admin|sk-agy-` = **0 命中** ✓。
- 三闸 + web 套件：`npm run check`=0 / `npm run build`=0（tsdown + web 链式）/ `npx vitest run` **320 passed (31 files)** / `npm run web:test` **15 passed (4 files)** / web check=0。

## M4 DoD 状态总表（acceptance.md §3）

| DoD | 状态 | 证据 |
|---|---|---|
| 六页面全部可用 + i18n 切换完整（grep 纪律） | ◐ 页面已交付且 grep/键集纪律自动化通过；**人工走查待用户** | 本文 ⑥、test/i18n completeness |
| SSE 实时推送免刷新更新 | ✓（curl -N 活体验证 snapshot/run/续传/回退；页面经 useAdminEvents 同通道）+ **浏览器端目视待用户** | 本文 ③ |
| 深浅主题、375px 可用（截图） | ✗ **待用户截图核对**（实现面已内建：pre-paint 主题、375px 单列断点） | — |
| 未认证 → 401；skip 二次确认 | ◐ 401 矩阵自动化 ✓；**skip 对话框人工走查待用户** | 本文 ②⑥d |
| Lighthouse a11y ≥90 | ✗ **待用户跑分**（a11y 纪律已内建：Label/焦点环/对比度/aria） | — |

**结论**：M4 交付面（web/ 六页面、事件总线 + SSE、设置写路径、静态托管、415 修复、CI/Docker 链路）在 fake-agy 演练与 320+15 项自动化测试下达标；浏览器人工项（走查/截图/Lighthouse/skip 对话框目视）待用户执行，未做任何发布动作。遗留至 M5：每 key 模型白名单 enforcement、账本 errorText 列（schema v2）、TanStack Virtual 再评。